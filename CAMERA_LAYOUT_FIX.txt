// FIX: Move flip camera button to bottom center next to capture button

// In index.html, find lines 82-92 (camera overlay):

// FIND:
                    <div class="camera-overlay">
                        <div style="position: absolute; top: 1rem; right: 1rem;">
                            <button id="flipCameraBtn" class="icon-btn" title="Chuyển camera"
                                style="width: 40px; height: 40px;">🔄</button>
                        </div>
                        <div class="camera-controls">
                            <button id="captureBtn" class="capture-btn">
                                <span class="capture-circle"></span>
                            </button>
                        </div>
                    </div>

// REPLACE WITH:
                    <div class="camera-overlay">
                        <div class="camera-controls">
                            <button id="flipCameraBtn" class="flip-camera-btn" title="Chuyển camera">
                                🔄
                            </button>
                            <button id="captureBtn" class="capture-btn">
                                <span class="capture-circle"></span>
                            </button>
                        </div>
                    </div>

// This moves flip button from top-right to bottom center, next to capture button
